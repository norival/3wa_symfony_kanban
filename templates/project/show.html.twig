{% extends 'base.html.twig' %}

{% block title %}Hello ProjectController!{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ project.title }}</h1>
        <p>{{ project.description }}</p>
    </div>
{% endblock %}
